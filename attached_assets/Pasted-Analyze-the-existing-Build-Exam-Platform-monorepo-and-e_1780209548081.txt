Analyze the existing Build-Exam-Platform monorepo and extend it with a complete production-ready Admin Management System.

The project already contains:

* Next.js App Router frontend (`artifacts/exam-platform`)
* Express.js API server (`artifacts/api-server`)
* Clerk Authentication
* Drizzle ORM database package (`lib/db`)
* OpenAPI specification generation
* Generated React API client
* Existing quiz, mock test, PYQ, NCERT, leaderboard, current affairs, study notes, and streak modules

Do NOT rebuild the existing system.

Instead, integrate the new functionality into the current architecture while following existing coding patterns and folder structures.

---

# ADMIN PANEL

Create a protected admin area under:

src/app/(admin)/admin

with:

* Dashboard
* Question Management
* Exam Management
* Student Management
* Analytics
* Draft Management
* Activity Logs
* Settings

Use the existing Clerk authentication system and implement admin role checks.

---

# QUESTION MANAGEMENT

Add complete CRUD operations:

* Create Question
* Edit Question
* Delete Question
* Archive Question
* Bulk Upload Questions
* Import CSV/Excel
* Export Questions

Question Types:

* Single Choice
* Multiple Choice
* True/False
* Fill in the Blank
* Subjective

Fields:

* Question
* Options
* Correct Answer
* Explanation
* Difficulty
* Subject
* Chapter
* Tags
* Marks
* Negative Marks
* Images

---

# AUTO DRAFT SYSTEM

Implement a robust draft system.

Whenever an admin is:

* Creating a question
* Editing a question
* Creating an exam
* Editing an exam
* Editing instructions
* Uploading content

The data should automatically save as draft.

Requirements:

* Save every 3-5 seconds
* Save on field change
* Restore after refresh
* Restore after browser crash
* Show draft status indicator
* Manual save draft button
* Publish button
* Draft recovery screen

Database tables:

* question_drafts
* exam_drafts

Drafts should be separate from published content.

---

# REDUX TOOLKIT

Use Redux only for:

* Authentication state
* Admin user state
* Sidebar state
* Theme state
* Global notifications
* Permissions
* Unsaved changes tracking

Create:

store/
├── store.ts
├── hooks.ts
└── slices/
├── authSlice.ts
├── adminSlice.ts
├── uiSlice.ts
├── draftSlice.ts
└── notificationSlice.ts

---

# REACT QUERY

Use React Query for all API communication.

Create:

src/lib/react-query

Requirements:

* Query Keys
* Infinite Queries
* Mutations
* Optimistic Updates
* Prefetching
* Pagination
* Cache Invalidation

Examples:

* questions
* exams
* students
* analytics
* drafts

Recommended defaults:

staleTime: 5 minutes
gcTime: 10 minutes

Implement loading and error boundaries.

---

# BACKEND API

Extend the existing Express server.

Create routes:

src/routes/admin

Structure:

admin/
├── dashboard.ts
├── questions.ts
├── drafts.ts
├── exams.ts
├── students.ts
├── analytics.ts
└── activityLogs.ts

Implement:

GET /admin/dashboard
GET /admin/questions
POST /admin/questions
PATCH /admin/questions/:id
DELETE /admin/questions/:id

GET /admin/drafts
POST /admin/drafts
PATCH /admin/drafts/:id

GET /admin/exams
POST /admin/exams

GET /admin/students

GET /admin/analytics

---

# DATABASE

Extend the existing Drizzle schemas.

Add:

questions
question_options
question_drafts
exams
exam_questions
exam_drafts
student_attempts
activity_logs
admin_notifications

Include:

* indexes
* foreign keys
* timestamps
* soft delete support

---

# SERVER CACHING

Use Node Cache.

Create:

src/lib/cache.ts

Cache:

Dashboard Statistics → 5 min
Question Lists → 10 min
Analytics → 15 min
Leaderboard → 5 min

Implement:

* cache middleware
* cache invalidation
* cache warmup
* cache hit logging

---

# SECURITY

Add production-grade security.

Implement:

Helmet
Rate Limiting
CORS
Input Sanitization
Zod Validation

Rate Limits:

Login → 5/min
Question Creation → 20/min
Exam Creation → 10/min
General APIs → 100/min

Use:

* HTTP-only cookies
* Secure cookies
* Clerk middleware protection
* Admin role middleware
* Audit logging

---

# ANALYTICS

Create admin analytics:

* Total Students
* Total Exams
* Total Questions
* Daily Active Users
* Exam Attempts
* Pass Percentage
* Top Scorers
* Subject Performance

Use charts and tables.

---

# UI REQUIREMENTS

Use existing ShadCN components already present in the project.

Create:

* Data Tables
* Search
* Filters
* Bulk Actions
* Pagination
* Command Menu
* Responsive Sidebar
* Breadcrumbs
* Loading Skeletons

Follow the existing design system.

---

# DELIVERABLES

Generate:

1. Updated folder structure
2. Drizzle schemas
3. Express routes
4. React Query setup
5. Redux Toolkit setup
6. Draft auto-save implementation
7. Node Cache implementation
8. Security middleware
9. Admin layouts
10. Admin pages
11. API integration
12. Database migration files

The solution must integrate with the existing codebase instead of replacing it, and should follow the current project architecture and conventions.
