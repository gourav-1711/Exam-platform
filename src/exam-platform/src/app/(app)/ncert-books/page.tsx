import NcertBooks from "@/views/NcertBooks";

export default function NcertBooksPage() {
  return <NcertBooks />;
}
</dyad-command>

<dyad-command type="refresh"></dyad-command>
<dyad-write path="src/exam-platform/src/app/(app)/ncert-books/page.tsx" description="Updating the ncert-books page to use the new view component">
import NcertBooks from "@/views/NcertBooks";

export default function NcertBooksPage() {
  return <NcertBooks />;
}